# Requirement

* JDK 17 and above
* IDE such as IDEA
* Docker and Docker compose

# Setup Environment

run commands in your project directory:

```shell
$ docker compose up -d
# or use ide
$  gradle flywayMigrate -i
```
